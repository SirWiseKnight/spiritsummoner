import 'package:flutter/material.dart';

class ChatScreen2 extends StatelessWidget {
  const ChatScreen2({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('Chat Screen 2');
  }
}
